module compare (
    input z,
    input v,
    input n,
    input alufn[6],
    output cmp
) {
    
    always {

        case(alufn[2:1]){
            b01: cmp = z            // equal
            b10: cmp = n ^ v        // less than

            default: cmp = 0
        }        
    }
}